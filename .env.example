# Copy to .env (never committed). Values below are LOCAL DEV ONLY.
QUANTLAB_ENVIRONMENT=development
QUANTLAB_DATABASE_URL=postgresql://quantlab:quantlab_local_only@localhost:5432/quantlab_dev
# Production is refused unless this explicit guard is set (Deployment Guide §6):
QUANTLAB_ALLOW_PRODUCTION=false
